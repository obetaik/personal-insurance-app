# .env file in your project root (same level as package.json)

# Remove test mode
# VITE_SKIP_AUTH=true

VITE_AUTH0_DOMAIN=de .com
VITE_AUTH0_CLIENT_ID=J k
VITE_AUTH0_CLIENT_SECRET=-zR fDg
VITE_AUTH0_AUDIENCE=http        # Must match exactly what you set in Auth0 API settings
VITE_API_URL=http://localhost:4000/api